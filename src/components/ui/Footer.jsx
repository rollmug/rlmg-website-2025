import React from "react";
import PropTypes from 'prop-types';

export const Footer = ({ className }) => {
    return (
        <footer className={`bg-primary text-neutral-content py-4 ${className}`}>
            
        </footer>
    );
}