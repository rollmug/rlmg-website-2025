import { Meta } from '@storybook/blocks';
import { Nunito_Sans } from "next/font/google";
import Image from "next/image";
import RLMG from './assets/rlmg-logo.png';

<Meta title="Introduction" />

<Image
        src={RLMG}
        alt="RLMG Logo"
        width={150}
        height={0}
        style={{ height: 'auto' }}
      />

# Rebrand 2025 UI Kit

## Components and layouts

<hr />

<div>
    <p class="nunito-sans">This is a collection of individual UI components and the functional layouts built with them.
        Each component is a standalone entity and can be used independently.
        The layouts are built using these components and are meant to be used as a whole.</p>
</div>

<style>
  {`
    .nunito-sans {
      font-size: 1.0rem;
      line-height: 1.8;
    }

    .sbdocs-wrapper {
      font-family: 'Nunito Sans', sans-serif;
      --dot-bg: white;
        --dot-color: #eaeef9;
        --dot-size: 2px;
        --dot-space: 38px;

        background: linear-gradient(
                90deg,
                var(--dot-bg) calc(var(--dot-space) - var(--dot-size)),
                transparent 1%
            )
            center / var(--dot-space) var(--dot-space),
            linear-gradient(
                var(--dot-bg) calc(var(--dot-space) - var(--dot-size)),
                transparent 1%
            )
            center / var(--dot-space) var(--dot-space),
            var(--dot-color) !important;
            background-attachment: fixed !important;
    }

    h1 {
      font-family: 'Nunito Sans', sans-serif;
      font-weight: 800 !important;
      color: #019FF9 !important;
    }

    h2 {
        border-bottom: none !important;
    }
    
    hr {
        border: 0;
        border-top: 6px solid #019FF9 !important;
        width: 40px;
        margin: 12px 0;
    }
  `}
</style>